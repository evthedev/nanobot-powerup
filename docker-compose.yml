x-nanobot-volume: &nanobot-volume
  - /opt/nanobot:/root/.nanobot

services:

  # ── Edge device bridge (:18790) + WhatsApp bridge (:3002) ─────────────────
  # Edge bridge: multi-device registry, per-device queues, HTTP sync + WS stream.
  # Device secrets are read from config.json — not env vars.
  nanobot-whatsapp-bridge:
    container_name: nanobot-whatsapp-bridge
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/usr/bin/node"]
    command: ["/app/bridge/dist/index.js"]
    restart: unless-stopped
    volumes: *nanobot-volume
    expose:
      - "3002"
      - "18790"
    ports:
      - "18790:18790"
    environment:
      - BRIDGE_PORT=3002
      - BRIDGE_HOST=0.0.0.0
      - BRIDGE_TOKEN=${BRIDGE_TOKEN:-}
      - AUTH_DIR=/root/.nanobot/whatsapp-auth
      - NANOBOT_MEDIA_DIR=/root/.nanobot/media
      - EDGE_DEVICES_ENABLED=${EDGE_DEVICES_ENABLED:-false}
      - REACHY_BRIDGE_PORT=18790
      - DB_PATH=/root/.nanobot/chat.db

  # ── nanobot gateway ─────────────────────────────────────────────────────────
  nanobot-gateway:
    container_name: nanobot-gateway
    build:
      context: .
      dockerfile: Dockerfile
    command: ["gateway"]
    restart: unless-stopped
    volumes: *nanobot-volume
    # Port 18791 (WebSocket) is internal only — dashboard talks to it directly
    expose:
      - "18791"
    environment:
      - PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium
      - PLAYWRIGHT_BROWSERS_PATH=/usr/bin
      - NANOBOT_SSL_VERIFY=false
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      - GH_REPO=${GH_REPO:-}
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # ── chat dashboard (Express API + React static) ───────────────────────────
  dashboard:
    container_name: nanobot-dashboard
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - /opt/nanobot:/root/.nanobot
      - /var/run/docker.sock:/var/run/docker.sock   # allows `docker restart` for gateway + WhatsApp bridge
    expose:
      - "3001"
    environment:
      - PORT=3001
      - NANOBOT_WS=ws://nanobot-gateway:18791
      - NANOBOT_HOME=/root/.nanobot
      - DB_PATH=/root/.nanobot/chat.db
      - DOCKER_API_VERSION=1.43   # Match host daemon so docker CLI can restart containers
      - BRIDGE_WS_URL=ws://nanobot-whatsapp-bridge:3002
      - BRIDGE_TOKEN=${BRIDGE_TOKEN:-}
    depends_on:
      - nanobot-gateway

  # ── nginx reverse proxy (HTTPS + basic auth) ─────────────────────────────
  nginx:
    container_name: nanobot-nginx
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/.htpasswd:/etc/nginx/.htpasswd:ro
      - ./deploy/nginx/entrypoint.sh:/entrypoint.sh:ro
      - nginx-ssl:/etc/nginx/ssl
    entrypoint: ["/bin/sh", "/entrypoint.sh"]
    depends_on:
      - dashboard

  # ── Claude Code web terminal (/claude) ───────────────────────────────────
  claude-terminal:
    container_name: nanobot-claude
    build:
      context: ./deploy/claude
      dockerfile: Dockerfile
    restart: unless-stopped
    working_dir: /workspace
    volumes:
      - /opt/nanobot-app:/workspace
      - /opt/nanobot:/root/.nanobot
    expose:
      - "7681"

  # ── one-off CLI (docker compose --profile cli run nanobot-cli <cmd>) ──────
  nanobot-cli:
    profiles: ["cli"]
    build:
      context: .
      dockerfile: Dockerfile
    volumes: *nanobot-volume
    command: ["status"]
    stdin_open: true
    tty: true

volumes:
  nginx-ssl:
    driver: local
